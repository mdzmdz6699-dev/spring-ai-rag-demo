数据格式规格：
系统统一使用 JSON 进行数据交换。工单对象（Ticket）字段定义如下：
- ticketId：字符串，工单唯一编号；
- deviceNo：字符串，故障设备编号；
- priority：枚举，取值为 高 / 中 / 低；
- status：枚举，取值为 待派工 / 处理中 / 已闭环；
- createdAt：字符串，ISO8601 格式时间戳。
所有接口返回统一包裹结构：{ "code": 0, "message": "ok", "data": {} }。
